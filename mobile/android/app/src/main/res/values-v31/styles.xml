<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">@color/lgr_parchemin</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_vide</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/lgr_parchemin</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.
         
         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!--
            Fond de la fenêtre ENTRE le splash natif et la première image de
            Flutter. Il valait `?android:colorBackground` — le blanc du thème
            système en clair, le noir en sombre — et produisait un flash d'une
            couleur étrangère au milieu du lancement : parchemin, puis blanc,
            puis parchemin.

            C'est ce clignotement qui faisait percevoir DEUX écrans de
            démarrage là où l'on n'en voulait qu'un.
        -->
        <item name="android:windowBackground">@color/lgr_parchemin</item>
    </style>
</resources>
